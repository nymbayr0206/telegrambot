# temp1 Knowledge Base

A canonical structured description of the AI Global **temp1** template has been created at:

```
/opt/data/knowledge_bases/ai-global-temp1/
├── temp1_official.jpg          ← The actual template image (copy)
└── README.md                   ← Full transcription: layout, colors, rules, workflow
```

## What's in the KB

| Section | Content |
|---------|---------|
| Visual Design | Dark gradient (navy→blue→black), gold accents (#D7AB46), white text, Manrope font |
| Layout Elements | Logo (top-right), contact (bottom), badge (top), portrait slot, content area |
| Critical Rules | Immutable template, only text + portrait photo may be added |
| KIE Workflow | tmpfiles upload → image-to-image task → poll → download with curl |
| File Locations | Local paths + Supabase URL |
| See Also | reeltemp1 for 9:16 reels |

## When to Use

Load this reference when:
- A future session needs to understand what temp1 looks like visually (no vision model available)
- The user asks about temp1's layout, colors, or design elements
- You need to verify the exact dimensions (1254x1254), brand colors, or element positions
- The user says "transcribe temp1" or "save temp1 for knowledge base"
